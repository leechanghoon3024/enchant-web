import styled from '@emotion/styled';

export const WInlineblock = styled.div`
    max-width: 100%;
    display: inline-block;
    font-family: GmarketSans, sans-serif;
    word-break: keep-all;
`;

export const CategoryText = styled('a')`
    padding: 1.5rem 0.75rem;
    color: #fff;
    cursor: pointer;
    font-weight: 500;
    font-family: GmarketSans, sans-serif;
`;

export const TextSizeSmall = styled('h4')`
    font-size: 0.875rem;
    font-family: GmarketSans, sans-serif;
`;

export const TextSizeSmallP = styled('p')`
    font-size: 0.875rem;
    font-family: GmarketSans, sans-serif;
    color: #6b7280;
`;
export const TextWeightSemiBold = styled('div')`
    font-weight: 600;
    font-family: GmarketSans, sans-serif;
`;
